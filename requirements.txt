# Minimal requirements for FRIDAY (local, very-low tier)
# Note: sentence-transformers installs transformers and torch which can be large.
# Install only what you need for your setup.

gpt4all>=0.4.0
sentence-transformers>=2.2.2
numpy
